{# @var plugin \samuelreichor\llmify\Llmify #}
{# @var settings \samuelreichor\llmify\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    label: 'Cache TTL',
    instructions: 'How long to cache all llmify data.',
    id: 'cacheTtl',
    name: 'cacheTtl',
    value: settings.cacheTtl,
    errors: settings.getErrors('cacheTtl')
}) }}
